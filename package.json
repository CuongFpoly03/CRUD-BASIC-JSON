{
  "name": "lesson3",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npx json-server --watch db.json --port 8000",
    "auth": "json-server --watch db.json ./node_modules/json-server-auth --port 8000",
    "start": "concurrently \"npm run server\" \"npm run auth\" "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^1.0.0-alpha.23",
    "json-server-auth": "^2.1.0"
  },
  "devDependencies": {},
  "description": ""
}
